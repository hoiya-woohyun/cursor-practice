---
description: "기본 코딩 규칙 - 변수명 규칙과 로직 패턴에 중점"
globs: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx"]
alwaysApply: true
---

# 📝 기본 코딩 규칙

## 🎯 Verify 브랜치의 기본 코딩 규칙

이 브랜치에서는 **변수명 규칙과 로직 패턴**에 중점을 둡니다.

## 📚 학습 목표

- 명명 규칙의 중요성 이해
- if-else 조건문 패턴 활용
- 코드 가독성과 유지보수성 향상
- 팀 코딩 컨벤션 준수

## 🔄 규칙 파일 참조

이 파일은 다른 규칙 파일들과 함께 작동합니다:

- **변수명 규칙**: `variable-naming-rules.md` 참조
- **로직 패턴 규칙**: `logic-pattern-rules.md` 참조
- **검증 규칙**: `verification-rules.md` 참조

## 💡 코딩 원칙

### 1. 가독성 우선

- 코드는 한 번 작성되지만 여러 번 읽힙니다
- 명확하고 이해하기 쉬운 코드 작성

### 2. 일관성 유지

- 프로젝트 전체에서 동일한 규칙 적용
- 팀 내 코딩 컨벤션 준수

### 3. 교육적 목적

- 기본적인 프로그래밍 개념 이해
- 고차 함수보다는 기본 제어 구조 사용

### 4. 점진적 개선

- 작은 단위로 코드 개선
- 지속적인 리뷰와 피드백

## 🎯 브랜치별 특화 규칙

### Verify 브랜치 특화 사항

- 배열 변수명은 'List'로 끝나야 함
- filter 메서드 사용 금지
- if-else 문과 for loop 사용 권장
- 의미있는 변수명 사용 필수

## 📋 체크리스트

- [ ] 변수명 규칙 준수 확인
- [ ] 로직 패턴 규칙 준수 확인
- [ ] 코드 가독성 확인
- [ ] 팀 컨벤션 준수 확인
